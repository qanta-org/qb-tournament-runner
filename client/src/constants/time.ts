export const MS_PER_SECOND = 1000;

// How often to update "lockout remaining" UIs while a lockout is active.
export const REVEAL_LOCKOUT_TICK_INTERVAL_MS = 200;

// Decimal places for displaying remaining lockout seconds.
export const REVEAL_LOCKOUT_SECONDS_DECIMALS = 1;

